# 特需領域核心素養資料庫 & IGP 自動化填寫工具

這是一個雙效合一的專案，不僅提供了完整的「特需領域核心素養（情意、創造、領導、獨立研究）」結構化 JSON 資料庫，可廣泛應用於**教學設計、教案撰寫、觀課紀錄**；更結合了 Python 腳本，能將這些素養與課程計畫表（`.doc` 或 `.docx`）的資料自動化寫入至空白的 IGP（個別輔導計畫）表格中，大幅節省教師手動填寫的繁瑣工作。
## 執行與啟動方式

本專案採用 Python 腳本直接處理 Word 文件。主要的填寫腳本存放在 `output/` 資料夾中。
請確認已安裝 `python-docx` 以及 `pywin32` 模組：
```bash
pip install python-docx pywin32 markitdown
```
若要執行新的填寫，可參考 `output/` 內的 `.py` 腳本（例如 `fill_igp_research7.py`），或是直接將空白 IGP 檔案與課程計畫交由 AI 助理透過 `auto_fill_igp` Skill 自動處理。

## 開發歷史 (Changelog)

### [2026-06-30] 專案進度存檔
- 新增功能：將特需領域核心素養從 PDF 文件中結構化抽取為 `special_ed_competencies_J.json`，方便未來重複調用。
- 新增功能：封裝了 `auto_fill_igp` Skill，未來可自動化處理任何學年的空白 IGP 檔案。

### [2026-08-02] 核心素養資料庫架構大升級
- 新增功能：除了原本的國中 (J級) 外，完整擴充了國小 (E級) 與高中 (U級) 的特需領域資料庫。
- 架構升級：JSON 資料庫全面支援「核心素養」、「學習表現」與「學習內容」三大巢狀分類，大幅增加資料再利用性（例如可延伸應用於自動產生教案或觀課紀錄表）。
- 優化防呆：升級 `auto_fill_igp` 腳本，精準定位新版 JSON 路徑，確保自動填寫穩定無誤。
- 清理專案：移除本機測試檔案與私人文件，保持 GitHub Repo 乾淨開源。
